[user]
	name = Andrea Carraro
	email = me@andreacarraro.it

[github]
	user = toomuchdesign

[core]
	ignorecase = false
	pager = delta

# Syntax-highlighted, line-numbered diffs via delta (brew "git-delta").
# `navigate = true` lets n/N jump between files in the pager.
[interactive]
	diffFilter = delta --color-only

[delta]
	navigate = true
	line-numbers = true
	side-by-side = true
	# Make file paths / line numbers in the diff clickable, opening in VS Code.
	# iTerm2 renders OSC 8 hyperlinks; ⌘-click a path to jump to that line.
	hyperlinks = true
	hyperlinks-file-link-format = "vscode://file/{path}:{line}"
	# Color theme via a preset "feature" (colors, syntax-theme and hunk chrome
	# all come from the theme — no hand-tuned styles here). Presets are vendored
	# in themes.gitconfig (included below). Preview them all against a real diff
	# with `delta --show-themes`, or try one live with `git diff | delta
	# --features <name>`. Swap the active theme by moving the comment.
	features = arctic-fox
	# features = mellow-barbet

# Vendored from delta's official themes.gitconfig — defines the preset
# "features" referenced above.
[include]
	path = themes.gitconfig

[merge]
	conflictstyle = diff3
	tool = vscode

[push]
	autoSetupRemote = true

[pull]
	ff = only

[alias]
	### Aliases from: http://githowto.com/aliases
	type = cat-file -t
	dump = cat-file -p

	### Silvio GIT alias

	# Show the revision log graph using ASCII art
	lg = log --graph --all --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(bold white)— %an%C(reset)%C(bold yellow)%d%C(reset)' --abbrev-commit --date=relative

	# List all local feature branches sorted according to the latest change
	# date to give an overview which branches are under active development.
	stbr = "for-each-ref --sort=-committerdate --format='%1B[32m%(committerdate:iso8601) %1B[34m%(committerdate:relative) %1B[0;m%(refname:short)' refs/heads/"

	# Local branches not yet merged into origin/master — work still in flight.
	pending = branch --no-merged origin/master

# Machine-specific config (untracked, not version controlled).
# Kept last so its values override the ones above.
# Missing files are silently ignored by git.
[include]
	path = config.untracked

[diff]
	tool = vscode

[difftool "vscode"]
	cmd = code --wait --diff \"$LOCAL\" \"$REMOTE\"

[difftool]
	prompt = false

[mergetool "vscode"]
	cmd = code --wait \"$MERGED\"
